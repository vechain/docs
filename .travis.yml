language: node_js
node_js:
  - "10"
script:
  - npm run docs:build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: .vuepress/dist/
  github_token: $GH_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master